# 🚀 Desafío 1: Laptop Gamer Shop

¡Mi primer proyecto interactivo con JavaScript! Este proyecto es una **Single Page Application (SPA)** básica que simula la lógica de un carrito de compras para una laptop gamer.

## 🛠️ Tecnologías Usadas

* **HTML5**: Estructura semántica.
* **CSS3**: Diseño responsivo y estilizado (Layout con Flexbox).
* **JavaScript (Vanilla)**: Lógica de negocio y manipulación del DOM mediante eventos `onclick`.

## 🕹️ Funcionalidades

- [x] **Precio Dinámico**: El precio base se carga directamente desde el script.
- [x] **Contador de Cantidad**: Botones de incremento y decremento funcionales.
- [x] **Validación de Stock**: La cantidad no puede ser menor a cero (evita números negativos).
- [x] **Cálculo de Total**: El valor total a pagar se actualiza en tiempo real multiplicando la cantidad por el precio base.

---

## 📂 Estructura del Proyecto

```text
.
├── assets/
│   ├── css/
│   │   └── estilos.css
│   └── img/
│       └── notebook.png
├── src/
│   └── js/
│       └── index.js
├── index.html
└── .gitignore

## 🧠 Aprendizajes Clave

En este desafío puse en práctica conceptos fundamentales de programación:

- **Variables:** Manejo de `let` y constantes para almacenar estados.
- **Manipulación del DOM:** Uso de `document.querySelector` y `document.getElementById` para conectar JS con HTML.
- **Lógica Condicional:** Implementación de `if` para controlar el flujo de la aplicación.
- **Tipos de Datos:** Conversión de *Strings* a *Numbers* para realizar operaciones matemáticas.

Distribuido bajo la licencia MIT. 
Hecho con ❤️ por Nacho Robles
